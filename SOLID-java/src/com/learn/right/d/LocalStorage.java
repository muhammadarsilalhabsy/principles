package com.learn.right.d;

public class LocalStorage implements IDataStorage{

  @Override
  public void saveDataUser(User user) {
    // connect to local storage (MySQL, PSQL, ect..)
    // save
  }
}
